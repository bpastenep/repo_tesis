<p id="notice"><%= notice %></p>

<h1>Tipo Evaluacions</h1>

<table>
  <thead>
    <tr>
      <th>Tipo evaluacion</th>
      <th>Descripcion tipo evaluacion</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tipo_evaluacions.each do |tipo_evaluacion| %>
      <tr>
        <td><%= tipo_evaluacion.tipo_evaluacion_id %></td>
        <td><%= tipo_evaluacion.descripcion_tipo_evaluacion %></td>
        <td><%= link_to 'Show', tipo_evaluacion %></td>
        <td><%= link_to 'Edit', edit_tipo_evaluacion_path(tipo_evaluacion) %></td>
        <td><%= link_to 'Destroy', tipo_evaluacion, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tipo Evaluacion', new_tipo_evaluacion_path %>
