<%= form_for(tipo_evaluacion) do |f| %>
  <% if tipo_evaluacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_evaluacion.errors.count, "error") %> prohibited this tipo_evaluacion from being saved:</h2>

      <ul>
      <% tipo_evaluacion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipo_evaluacion_id %>
    <%= f.number_field :tipo_evaluacion_id %>
  </div>

  <div class="field">
    <%= f.label :descripcion_tipo_evaluacion %>
    <%= f.text_field :descripcion_tipo_evaluacion %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
