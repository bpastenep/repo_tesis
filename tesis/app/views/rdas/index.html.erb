<p id="notice"><%= notice %></p>

<h1>Rdas</h1>

<table class="table table-striped">
  <thead >
    <tr class="table-warning">
      <th>Nombre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @rdas.each do |rda| %>
      <tr>
        <td><%= rda.nombre %></td>
        
        <td><%= link_to 'Mostrar', rda %></td>
        <td><%= link_to 'Editar', edit_rda_path(rda) %></td>
        <td><%= link_to 'Eliminar', rda, method: :delete, data: { confirm: 'Está seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Rda', new_rda_path %>
