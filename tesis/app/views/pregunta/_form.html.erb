<%= form_for(preguntum) do |f| %>
  <% if preguntum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(preguntum.errors.count, "error") %> prohibited this preguntum from being saved:</h2>

      <ul>
      <% preguntum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pregunta_realizar %>
    <%= f.text_area :pregunta_realizar %>
  </div>

  <div class="field">
    <%= f.label :tipo_pregunta %>
    <%= f.number_field :tipo_pregunta %>
  </div>

  <div class="field">
    <%= f.label :respuesta %>
    <%= f.text_area :respuesta %>
  </div>

  <div class="field">
    <%= f.label :id_imagen %>
    <%= f.number_field :id_imagen %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
